<% if signed_in? %>
<%= render '/shared/sidebar' %>
  <div class="span9">
     <div class="center kevin-unit"> 
         <div id="chartdiv" style="width: 100%; height: 400px; background-color:white;"></div>
     </div>
  </div>  
</div><!--/.sidebar row -->
</div><!--/.sidebar container -->
<% else %>

<div class="center kevin-unit">

 <h1>あなたの英語のレベルを知っていますか？</h1>
 <a href="#" data-controls-modal="modal-from-dom" data-backdrop="true" class="video-play">play</a>
 <br/>
 <br/>

<%= link_to I18n.t(:free_trial), signup_path, class: "btn btn-large btn-primary" %>

<br/>

   <title>BiFluent</title>
    
        <div id="myCarousel" class="carousel slide">
            <div class="carousel-inner">
                <div class="item active">
                   <%= image_tag("Left.png", alt: "Step 1") %>
                </div>
                <div class="item">
                    <%= image_tag("Middle.png", alt: "Step 2") %>
                </div>
                <div class="item">
                  <%= image_tag("Right.png", alt: "Step 3") %>
                </div>
            </div>
            <!-- Carousel nav -->
            <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
            <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
        </div>
</div>
</div>
</div>
<% end %> 

<script type="text/javascript">
            var chart;

            var chartData = [{
                skill: "Sentence Structure",
                score: 5
            }, {
                skill: "Grammar",
                score: 4
            }, {
                skill: "Pronunciation",
                score: 6
            }, {
                skill: "Vocabulary",
                score: 4.5
            }, {
                skill: "Listening",
                score: 7.2
            }, {
                skill: "Accuracy",
                score: 6.1
            }, {
            skill: "Quick Reponse",
                score: 9.1
            }, {
            skill: "Fluency",
                score: 4.6
            }];

            AmCharts.ready(function () {
                // RADAR CHART
                chart = new AmCharts.AmRadarChart();
                chart.dataProvider = chartData;
                chart.categoryField = "skill";
                chart.startDuration = 1;

                // TITLE
                chart.addTitle("My BiFluent", 15);

                // VALUE AXIS
                var valueAxis = new AmCharts.ValueAxis();
                valueAxis.fillAlpha = 0.05;
                valueAxis.axisAlpha = 0.15;
                valueAxis.fillColor = "#000000";
                valueAxis.dashLength = 3;
                valueAxis.minimum = 0;
                valueAxis.gridCount = 5;
                chart.addValueAxis(valueAxis);

                // GRAPH
                var graph = new AmCharts.AmGraph();
                graph.fillAlphas = 0.4;
                graph.fillColor = "#7fc837";
                graph.valueField = "score";
                graph.bullet = "round";
                graph.balloonText = "BiFluent Score: [[value]]"
                chart.addGraph(graph);

                // WRITE
                chart.write("chartdiv");
            });
        </script>
       
  <script charset="utf-8" type="text/javascript">
    $(function() {
        $('.video-play').click(function() {        
            $('.my-modal').on('show', function() {
              $('.my-modal').html($('#modal-contents').html());
            }).on('hidden', function() {
              $('.my-modal').html('');
            }).modal({
                backdrop: true,
                keyboard: true
            }).css({
                width: 'auto',
                'margin-left': function () {
                    return -($(this).width() / 2);
                }
            });    
        });
      
       
    });
  </script>

    <!-- modal content -->
    <script type="text/html" id="modal-contents">
      <iframe src="https://player.vimeo.com/video/39962822?byline=0&amp;portrait=0&amp;color=62d94e&amp;autoplay=1" width="750" height="422" frameborder="0" webkitAllowFullScreen mozallowfullscreen allowFullScreen></iframe>
    </script>
  
    <div id="modal-from-dom" class="modal fade my-modal">
    </div>







